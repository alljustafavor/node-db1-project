const getAll = () => {
  return "getAll wired"
}

const getById = id => {
  return "GetByID wired"
}

const create = account => {
  return "Create wired"
}

const updateById = (id, account) => {
  return "UpdateByID wired"
}

const deleteById = id => {
  return "delete wired"
}

module.exports = {
  getAll,
  getById,
  create,
  updateById,
  deleteById,
}
